{
  "name": "frost-route",
  "version": "1.0.0",
  "private": true,
  "description": "MQTT와 TimescaleDB 기반 콜드체인 차량 관제 프로젝트",
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=24.18.0 <25",
    "pnpm": "11.15.1"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "db:migrate": "pnpm --filter @frost-route/database migrate",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "infra:compose": "docker compose --env-file .env -f infrastructure/docker/compose.yml",
    "infra:config": "docker compose --env-file .env.example -f infrastructure/docker/compose.yml config",
    "infra:down": "pnpm infra:compose down",
    "infra:logs": "pnpm infra:compose logs --follow",
    "infra:ps": "pnpm infra:compose ps",
    "infra:up": "pnpm infra:compose up --detach --wait",
    "lint": "eslint .",
    "prepare": "husky",
    "test": "vitest run --passWithNoTests",
    "test:integration": "pnpm --filter @frost-route/database test:integration",
    "typecheck": "tsc --noEmit -p tsconfig.json && pnpm -r --if-present typecheck"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,mjs,cjs}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": "prettier --write",
    "*.tf": "terraform fmt"
  },
  "devDependencies": {
    "@eslint/js": "catalog:",
    "@types/node": "catalog:",
    "eslint": "catalog:",
    "husky": "catalog:",
    "lint-staged": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:",
    "vitest": "catalog:"
  }
}
